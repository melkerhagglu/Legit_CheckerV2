package com.legitchecker.model;

public class MainModel {
    // This is the Model in MVC pattern
    // Currently empty but can be extended with business logic and data
    
    public MainModel() {
        // Initialize model
    }
}
